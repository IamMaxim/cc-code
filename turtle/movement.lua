

function newPositionInfo()
    PositionInfo = {
        x = 0,
        y = 0,
        z = 0,
        rot = "forward"
    }

    PositionInfo.forward = function()
        succ = turtle.forward()
        if succ the
    end

    return PositionInfo
end